import { ParallaxButtonDirective } from './parallax-button.directive';

describe('ParallaxButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ParallaxButtonDirective();
    expect(directive).toBeTruthy();
  });
});
